# name: Database Backup Workflow

# # Define when this workflow should run
# on:
#   # Trigger on pushes to dev, master, or main branches
#   push:
#     branches:
#       - dev
#       - master
#       - main
#   # Allows you to run this workflow manually from the Actions tab
#   workflow_dispatch:
#     inputs:
#       database:
#         description: 'Database name'
#         required: false
#         default: 'recruitment'
#       host:
#         description: 'Database host'
#         required: false
#         default: 'studio-db.valtunox.com'
#       port:
#         description: 'Database port'
#         required: false
#         default: '5432'
#       s3_bucket:
#         description: 'S3 bucket name for backup storage'
#         required: false
#         default: 'data.valtunox.com'
#       s3_prefix:
#         description: 'S3 prefix/path for backups'
#         required: false
#         default: 'backups'
#   # Run on a schedule: every 2 hours
#   schedule:
#     - cron: '0 */2 * * *'

# # Define the jobs to run
# jobs:
#   backup-database:
#     runs-on: ubuntu-latest
    
#     steps:
#       # Step 1: Checkout the repository
#       - name: Checkout repository
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       # Step 2: Install PostgreSQL client tools (includes pg_dump, psql, etc.)
#       - name: Install PostgreSQL client tools
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y postgresql-client

#       # Step 3: Set up Python 3.11 (minimal setup for backup script)
#       - name: Set up Python 3.11
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'
#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install psycopg2-binary python-dotenv requests boto3 asyncpg fastapi

#       # Step 5: Run the database backup script (uses pg_dump from PostgreSQL client tools)
#       - name: Run database backup script
#         env:
#           POSTGRES_HOST: ${{ secrets.POSTGRES_HOST || github.event.inputs.host || 'studio-db.valtunox.com' }}
#           POSTGRES_PORT: ${{ secrets.POSTGRES_PORT || github.event.inputs.port || '5432' }}
#           POSTGRES_DB: ${{ secrets.POSTGRES_DB || github.event.inputs.database || 'recruitment' }}
#           POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
#           POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
#         run: |
#           echo "📦 Starting database backup..."
#           python scripts/database/backup_database.py \
#             --database ${{ secrets.POSTGRES_DB || github.event.inputs.database || 'recruitment' }} \
#             --host ${{ secrets.POSTGRES_HOST || github.event.inputs.host || 'studio-db.valtunox.com' }} \
#             --port ${{ secrets.POSTGRES_PORT || github.event.inputs.port || '5432' }} \
#             --user ${{ secrets.POSTGRES_USER }} \
#             --password ${{ secrets.POSTGRES_PASSWORD }} \
#             --formats custom plain \
#             --skip-csv \
#             --skip-excel
#           echo "✅ Database backup completed successfully"

#       # Step 6: Configure AWS credentials
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1

#       # Step 7: Upload .dump and .sql files to S3
#       - name: Upload backups to S3
#         run: |
#           echo "📤 Uploading database backups to S3..."
          
#           BACKUP_DIR="app/data/backups/postgres"
#           S3_BUCKET="${{ github.event.inputs.s3_bucket || 'data.valtunox.com' }}"
#           S3_PREFIX="${{ github.event.inputs.s3_prefix || 'backups' }}"
#           TIMESTAMP=$(date -u +"%Y%m%d_%H%M%S")
          
#           # Upload all .dump and .sql files to S3
#           echo "Uploading .dump files..."
#           aws s3 cp "$BACKUP_DIR/" "s3://${S3_BUCKET}/${S3_PREFIX}/" \
#             --recursive \
#             --exclude "*" \
#             --include "*.dump" \
#             --metadata "uploaded-by=github-actions,workflow=database-backup,timestamp=${TIMESTAMP},database=${{ github.event.inputs.database || 'recruitment' }}"
          
#           echo "Uploading .sql files..."
#           aws s3 cp "$BACKUP_DIR/" "s3://${S3_BUCKET}/${S3_PREFIX}/" \
#             --recursive \
#             --exclude "*" \
#             --include "*.sql" \
#             --metadata "uploaded-by=github-actions,workflow=database-backup,timestamp=${TIMESTAMP},database=${{ github.event.inputs.database || 'recruitment' }}"
          
#           echo "✅ Upload complete!"
#           echo "📁 Files uploaded to: s3://${S3_BUCKET}/${S3_PREFIX}/"
#           echo "🔗 S3 Console: https://s3.console.aws.amazon.com/s3/buckets/${S3_BUCKET}?prefix=${S3_PREFIX}/"
          
#           # List uploaded files (last 10)
#           echo ""
#           echo "📋 Uploaded backup files:"
#           aws s3 ls "s3://${S3_BUCKET}/${S3_PREFIX}/" --recursive | grep -E "\.(dump|sql)$" | tail -10

#       # Step 8: Backup Summary
#       - name: Backup Summary
#         if: success()
#         run: |
#           echo "## 💾 Database Backup Completed Successfully" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "**Database:** ${{ github.event.inputs.database || 'recruitment' }}" >> $GITHUB_STEP_SUMMARY
#           echo "**Host:** ${{ github.event.inputs.host || 'studio-db.valtunox.com' }}" >> $GITHUB_STEP_SUMMARY
#           echo "**S3 Bucket:** ${{ github.event.inputs.s3_bucket || 'data.valtunox.com' }}" >> $GITHUB_STEP_SUMMARY
#           echo "**S3 Prefix:** ${{ github.event.inputs.s3_prefix || 'backups' }}" >> $GITHUB_STEP_SUMMARY
#           echo "**Backup Time:** $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "### 📦 Backup Files:" >> $GITHUB_STEP_SUMMARY
#           echo "- `.dump` files (custom format) - Use with pg_restore" >> $GITHUB_STEP_SUMMARY
#           echo "- `.sql` files (plain format) - Use with psql" >> $GITHUB_STEP_SUMMARY
#           echo "- `recruitment_latest_updated.sql` - Latest SQL backup (default for restore)" >> $GITHUB_STEP_SUMMARY
#           echo "- `recruitment_latest_updated.dump` - Latest dump backup" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "### 🔗 S3 Location:" >> $GITHUB_STEP_SUMMARY
#           echo "https://s3.console.aws.amazon.com/s3/buckets/${{ github.event.inputs.s3_bucket || 'data.valtunox.com' }}?prefix=${{ github.event.inputs.s3_prefix || 'backups' }}/" >> $GITHUB_STEP_SUMMARY

#       - name: Send failure notification
#         if: failure()
#         run: |
#           echo "## ❌ Database Backup Failed" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "**Database:** ${{ github.event.inputs.database || 'recruitment' }}" >> $GITHUB_STEP_SUMMARY
#           echo "**Time:** $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "Please check the workflow logs for details." >> $GITHUB_STEP_SUMMARY

